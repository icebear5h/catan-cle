# SFT Runs

Local checkpoints, adapter weights, optimizer state, and logs belong here and
are ignored by Git. Keep only compact accepted run specifications, summaries,
and provenance manifests under version control; store large weights in the
configured remote run volume or external artifact storage.
