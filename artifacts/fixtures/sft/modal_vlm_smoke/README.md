# Modal VLM Smoke Fixture

A self-contained 24-row, four-image fixture used by the 2026-05-14 Modal SFT
smoke runs. Image references in `train.jsonl` are relative to this directory.
This is an infrastructure fixture, not a model-quality benchmark.
