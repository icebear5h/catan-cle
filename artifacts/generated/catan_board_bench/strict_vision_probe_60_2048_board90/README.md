# Strict 60-question raw-vision probe

This is the raw-image projection of `text_format_optimization_probe`. The engine public-state contracts are the oracle and are rendered as ordinary unannotated 2048px board screenshots. Text-only opaque aliases are inverted to canonical engine IDs in the questions and strict answers. The semantic targets, question IDs, board states, categories, and scorer are unchanged.
