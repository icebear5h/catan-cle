BASELINE (w2-scripts): ruff 128, mypy 285, structure 20 files, pytest 230 passed / 33 skipped / 11 errors (errors all tests/data_pipeline/recognition/export/test_reweight_node_edge.py, not my area). Collection-broken elsewhere: tests/evals/board_bench/test_catan_board_bench.py (evals), tests/test_verify_spatial_extension.py + tests/test_eval_qwen_text.py + tests/sft/** (sft).
DONE scripts/verify_data_pipeline_layout.py (165L) + scripts/verify_sft_layout.py (216L): TypedDicts replace Any; ruff/mypy clean; 11 tests pass; --help identical.
DONE scripts/compact_live_traces.py (162L): Callable log params, buffer narrowing replaces type:ignore; ruff/mypy clean; 2 tests pass; --help identical.
DONE scripts/time_live_game.py (220L): typed emit/_turns_played helpers for the unannotated playground viewer; ruff/mypy clean; --help identical.
DONE scripts/render_catan_inspect_viz.py (269L): TypedDict archive records + boundary cast; metadata None guarded; ruff/mypy clean; tests pass.
DONE scripts/deploy_glm_api.py (296L, was 280 over-lint): container imports moved to Modal image.imports() (repo pattern), narrow Protocols for the unannotated processor, dead imports dropped, aiohttp ClientTimeout. Ruff clean; structure OK; 5 mypy left, ALL from fastapi not being installed (2 import-not-found + 3 derived untyped-decorator).
DONE scripts/import_catan_inspect_archives.py (209L) + NEW scripts/archive_import/{__init__.py 1L, output.py 170L}: ownership/layout helpers extracted, names re-exported with __all__; ruff/mypy clean; 5 tests pass; both `python scripts/...py --help` and `python -m scripts....` work.
DONE scripts/probes/inspect_board_pixels.py (143L): Pillow font union return type; ruff/mypy clean; -m --help works.
DONE scripts/probes/vlm_benchmark.py -> package scripts/probes/vlm_benchmark/{__init__ 53L, __main__ 5L, catalog 86L, cli 137L, providers 139L}: ModelResult TypedDict, prog kept as vlm_benchmark.py so --help text is byte-identical; ruff/mypy clean.
DONE scripts/probes/vlm_benchmark_modal.py (296L, was 366) + NEW scripts/probes/vlm_prompts.py (51L) + scripts/probes/modal_typing.py (58L): container imports via image.imports(), TypedDict/Protocol handles, prompts+registry extracted. Ruff clean; 2 mypy left, both pre-existing modal-decorator issues (Secret.from_name required_hint, VLMBenchmark(model_id=)).
--- RESUMED (opus) baseline: ruff 62, mypy 237, structure 17. pytest blocked at collection by playground get_player_resources (w2-playground), not mine.
DONE NEW scripts/reasoning/jsonio.py (102L): typed JSON artifact helpers (read/write/append json+jsonl, utc_now, object_list, string_list) on evals.json_types JsonDict/JsonValue. Replaces five duplicated private helpers across the reasoning scripts.
DONE scripts/reasoning/complete_transcript_observation_assembly_with_pi.py (317L -> 293L): under 300; packet builder extracted to _input_packet; JsonDict payloads; ObservationAssemblyJob typed; ruff/mypy clean; -m --help works.
DONE scripts/reasoning/complete_transcript_reasoning_with_pi.py (265L -> 243L): same treatment, ReasoningJob typed; ruff/mypy clean; -m --help works.
DONE scripts/reasoning/build_transcript_reasoning.py (88L -> 82L) + build_transcript_observation_assembly.py (96L -> 90L): dropped duplicated _write_plan for jsonio.write_json; ruff/mypy clean; -m --help works.
DONE scripts/reasoning/build_decision_spot_checks.py (119L -> 102L): dropped duplicated _write_jsonl/_write_json/utc clock for jsonio; JsonDict annotations replace Any; ruff/mypy clean; -m --help works.
DONE scripts/reasoning/eval_catan_initial_settlement_reasoning.py (709L) -> package scripts/reasoning/eval_catan_initial_settlement_reasoning/{__init__ 72L, __main__ 8L, artifacts 143L, capture 143L, cli 218L, costs 76L, reports 209L, seeds 179L}. All Any removed via cle.players.data.JsonValue + json_object/json_text/json_int/json_float/json_bool/json_payload narrowing; PlayerAttempt/PlayerChoice/httpx.AsyncClient replace Any params; index rows became a frozen TraceRow dataclass so the summary sums are typed. prog+description pinned so --help is byte-identical to the pre-split module (diffed). 4 tests pass. scripts/reasoning now 0 ruff / 0 mypy / 0 structure.
DONE scripts/probes/probe_catan_board_parts.py (492L) -> package scripts/probes/probe_catan_board_parts/{__init__ 76L, __main__ 8L, tokens 158L, failures 209L, stats 194L, report 95L, cli 78L}. Any gone: JsonDict on cle.players.data.JsonValue, FailureExample TypedDict, PartStats/Totals/Analysis dataclasses replace the defaultdict-of-Any aggregation. Fixed the re.search(...).group() union-attr errors by binding the match once. Dropped the dead PROJECT_ROOT/sys.path hack (file imports only stdlib). prog+description pinned. Verified stdout AND the --out JSON byte-identical to the HEAD script on 61 synthetic rows covering every category plus an error row, both unfiltered and with --model/--top-failures.
QUIRK (pre-existing, preserved): color_building_counts used r"SETTLEMENTS\\s+(\\d+)" - a raw string with a doubled backslash, so the regex wants a literal backslash and never matches real text. exp_settlements/exp_cities are therefore always None. Kept byte-for-byte in failures._labelled_count; flag to whoever owns this probe.
DONE scripts/probes/probe_catan_board_coverage.py (657L) -> package scripts/probes/probe_catan_board_coverage/{__init__ 67L, __main__ 8L, model 179L, inventory 155L, evaluate 205L, report 181L, cli 167L}. Any gone via TypedDicts that serialize verbatim (ProbeEntry/ProbeResult/PartPayload/CoverageMetrics/PartSummary/GeneratedFrom/Report) on cle.players.data.JsonValue; the dict[str|None, ...] key errors fixed by building the inventory with explicit token-skipping loops. Dropped the dead PROJECT_ROOT/sys.path hack. prog+description pinned. Verified stdout AND --out JSON byte-identical to the HEAD script on synthetic fixtures across 11 invocations: default, all five --part values, both --tile-mode values, --model/--top, --image (path and basename), --eval-dir; --help identical too.
DONE scripts/probes/probe_catan_board_mech_geometry.py (822L) -> package scripts/probes/probe_catan_board_mech_geometry/{__init__ 57L, __main__ 8L, constants 42L, rows 283L, atlas 132L, model_io 258L, metrics 169L, cli 153L}. Any gone: ProbeRow/TokenIndexMaps TypedDicts, narrow Protocols (Tokenizer/Processor/ModelInputs/VisionModel/Embeddings/ModelOutputs) for the untyped transformers objects, npt.NDArray[np.float32] for the numpy work. The inline `from qwen_vl_utils import process_vision_info` (PLC0415) became importlib.import_module inside the function, matching the file's own peft/transformers pattern, so the optional untyped dep stays lazy and needs no stub. prog+description pinned; --help byte-identical. Verified load_manifest/load_rows identical to HEAD across 8 --parts/--rows-limit combos on 120 synthetic QA rows, plus token_index_maps, build_atlas_graphs and find_subsequence_indices exact-equal.
scripts/probes now 0 ruff / 0 structure; mypy only the 2 known pre-existing Modal decorator errors in vlm_benchmark_modal.py (reproduce on HEAD).
DONE scripts/board_recognition/ import-order autofixes on 7 export/build scripts (I001 only, no logic touched).
DONE scripts/board_recognition/build_catan_board_recognition_eval_suite.py (248L -> 280L): Any gone via JsonDict on cle.players.data.JsonValue plus _object/_objects/_values narrowing; read_jsonl now rejects non-object rows. 20 tests pass.
DONE scripts/board_recognition/build_catan_board_recognition_curriculum.py (974L) -> package scripts/board_recognition/build_catan_board_recognition_curriculum/{__init__ 128L, __main__ 8L, shapes 118L, paths 38L, jsonio 93L, spec 67L, contracts 157L, counterfactual 172L, labels 172L, metadata 124L, validate 194L, checks 148L, build 242L, cli 51L}. All Any gone: JsonDict + obj/objs/values/strings/text/integer/number/edge_pair narrowing, BoardIndices=dict[str,object] with edge_ids_from for the non-JSON builder index, RenderStyle kwargs coerced to float. Fixed the pre-existing tuple[int,...] vs tuple[int,int] roads errors by parsing edge ids through edge_pair. PROJECT_ROOT moved to parents[3] for the new depth. prog+description pinned. 6 tests pass, including the end-to-end build_dataset + validate_dataset round trip.
scripts/board_recognition now 0 ruff / 0 structure.
BLOCKED (not mine): data_pipeline/board_recognition/sources/ is a package directory with _audit.py _config.py _contracts.py _digests.py _lock.py and NO __init__.py, so mypy reports attr-defined errors at the import sites in scripts/board_recognition/audit_catan_board_recognition_sources.py (7) and build_catan_board_recognition_dataset.py (1). Half-done split in w2-data-pipeline's area; needs their __init__ re-export.
DONE scripts/board_bench/ import-order autofixes on 14 files (I001 only).
DONE NEW scripts/board_bench/shapes.py (131L): JsonDict on cle.players.data.JsonValue plus obj/objs/values/strings/text/integer/number narrowing, read_json/read_json_object/read_jsonl, write_json/write_jsonl/write_text, markdown_table. Shared by every board_bench package so the helpers are written once.
DONE scripts/board_bench/builders/build_catan_board_bench_leakage_and_presft.py (398L) -> package .../build_catan_board_bench_leakage_and_presft/{__init__ 71L, __main__ 8L, paths 49L, load 168L, render 185L, cli 77L}. All Any gone via scripts/board_bench/shapes; ROOT moved to parents[4]; prog+description pinned; -m --help verified and ROOT/BENCH_DIR resolve correctly.
DONE scripts/board_bench/builders/render_catan_strict_vision_probe.py (399L) -> package .../render_catan_strict_vision_probe/{__init__ 62L, __main__ 8L, constants 42L, source 118L, projection 124L, render 176L, cli 55L}. All Any gone via scripts/board_bench/shapes; prog+description pinned; -m --help verified. 45 board_bench-importing tests pass.
NOTE: tests/evals/board_bench/test_catan_board_bench_naming.py cannot be collected - playground/game_viewer/routes/prompt_studio/errors.py raises "RuntimeError: super(): no arguments" at import. w2-playground's area, not mine. Earlier the same suite was blocked by playground get_player_resources; that one cleared.
DONE scripts/board_bench/run/eval_catan_board_bench_text_formats.py (406L) -> package .../eval_catan_board_bench_text_formats/{__init__ 58L, __main__ 8L, constants 57L, transport 102L, summary 89L, run 230L, cli 86L}. All Any gone via scripts/board_bench/shapes. Used a local _numeric returning int|float instead of shapes.number so integer counters keep serializing as ints, not 5.0. load_dotenv() stays at constants import time. prog+description pinned; -m --help verified.
DONE scripts/board_bench/run/run_catan_openrouter_model_sweep.py (409L) -> package .../run_catan_openrouter_model_sweep/{__init__ 50L, __main__ 8L, catalog 109L, costs 58L, commands 137L, status 74L, cli 129L}. All Any gone via scripts/board_bench/shapes. CAUGHT A REAL BUG in my own first pass: config["strategy_guided_reasoning"]["excluded_models"] is a dict (model_id -> DQ reason), not a list, so narrowing it with values() broke `model_id not in excluded`. Added shapes.membership() which accepts a JSON list or object and returns the set an `in` test would match; the sweep test caught it. 4 tests pass; prog+description pinned; -m --help verified.
DONE scripts/board_bench/builders/build_catan_board_bench_piece_visuals.py (732L) -> package .../build_catan_board_bench_piece_visuals/{__init__ 78L, __main__ 8L, constants 35L, writer 133L, images 182L, isolated 158L, patches 210L, build 68L, cli 51L}. All Any gone via scripts/board_bench/shapes; PROJECT_ROOT moved to parents[4] and verified; bbox/center lists wrapped with list() so they satisfy JsonValue without changing the JSON. prog+description pinned. 10 tests in tests/sft/data/test_sft_tooling.py pass.
DONE scripts/board_bench/run/eval_catan_board_bench_ascii_variations.py (594L) -> package .../eval_catan_board_bench_ascii_variations/{__init__ 71L, __main__ 8L, constants 96L, jobs 184L, records 124L, transport 82L, summary 97L, cli 173L}. All Any gone via scripts/board_bench/shapes (added shapes.numeric, int|float preserving, so token/score counters keep their int type). The package __init__ does `import httpx` and lists it in __all__ because the test patches "<module>.httpx.Client"; verified the patch still lands (8 tests pass). prog+description pinned.
DONE scripts/board_bench/run/eval_catan_board_bench_openrouter.py (1332L) -> package .../eval_catan_board_bench_openrouter/{__init__ 139L, __main__ 8L, constants 154L, text_norm 227L, questions 261L, transport 274L, scoring 209L, summary 66L, plan 153L, cli 243L}. All Any gone via scripts/board_bench/shapes. call_provider's `**kwargs: Any` (ANN401) replaced with explicit keyword parameters and an if/elif dispatch; the unknown-provider KeyError is still raised BEFORE the try so the request_interval sleep does not run, matching the old dict-lookup order. Moondream's `proc` now has a None guard for the unreachable empty-attempts case. __init__ re-exports httpx for the "<module>.httpx.Client" patch. prog+description pinned. 45 board_bench tests pass.
DONE scripts/board_bench/run/eval_catan_strict_vision_probe.py (784L) -> package .../eval_catan_strict_vision_probe/{__init__ 95L, __main__ 8L, constants 96L, dataset 136L, prompts 100L, jobs 170L, records 141L, summary 86L, execute 246L, cli 80L}. Jobs became a VisionJob TypedDict because they hold a live ImageBoardPresentation, not JSON. Any gone elsewhere via shapes. call_job's `caller = a if ... else b` plus **extra_kwargs replaced with an explicit if/else. worker_target's Callable[..., None] became a WorkerTarget Protocol with positional-only params (Callable[..., X] counts as explicit Any); the response-log handle became a ResultSink Protocol so no IO[Any] is needed; Connection is parameterized Connection[JsonDict, JsonDict]. prog+description pinned. 3 tests pass, including the hard-wall-clock-deadline test that injects its own worker_target.
DONE scripts/board_bench/run/eval_catan_board_bench_full_graph_formats.py (840L) -> package .../eval_catan_board_bench_full_graph_formats/{__init__ 136L, __main__ 8L, dataset_config 33L, constants 147L, dataset 178L, jobs 184L, records 152L, transport 82L, summary 110L, cli 229L}. LATE-BINDING: the seven names novita retargets (DATASET_SCHEMA, DEFAULT_DATASET_DIR, FORMAT_NAMES, FORMAT_EXTENSIONS, parse_full_graph_format, EVAL_SCHEMA, SUITE_NAME) now live alone in dataset_config.py; every reader resolves them as dataset_config.NAME at call time, and the package __init__ forwards reads via module __getattr__ so pkg.FORMAT_NAMES is never a stale snapshot. Updated configured_text_evaluator in the novita script to patch dataset_config instead of the package - patching the package would no longer reach the submodule readers. Jobs became a FormatJob TypedDict (they hold a live TextBoardPresentation). 9 tests across the novita and full-graph suites pass.
DONE scripts/board_bench/run/eval_catan_strict_text_probe_novita.py (482L) -> package .../eval_catan_strict_text_probe_novita/{__init__ 69L, __main__ 8L, constants 96L, transport 74L, plan 113L, records 151L, cli 180L}. All Any gone via shapes; reuses FormatJob from the full-graph package. configured_text_evaluator now lives in constants and patches text_evaluator.dataset_config. __init__ re-exports httpx for the "<module>.httpx.Client" patch. prog+description pinned. 2 tests pass.
DONE scripts/board_bench/run/eval_catan_tile_prompt_ablation.py (1092L) -> package .../eval_catan_tile_prompt_ablation/{__init__ 281L, __main__ 8L, defaults 146L, prompts 76L, questions 148L, plan 106L, transport 102L, records 148L, summary 219L, cli 219L}. LATE-BINDING: scorer_sha256 hashes ANGLE_CONDITIONS/ANGLE_NUMBER_RE out of its own module globals and the factor test patches them on the package, so the whole scorer core (condition sets, the four regexes, is_angle/has_descriptions/format_label/expected_response/parse_response/score_response/scorer_sha256, plus `import re`) stays in __init__.py; sibling modules are imported at the BOTTOM of __init__ so they can import those names back. Added TileTruth/ScoreDict/TileQuestion/TileJob TypedDicts and truth_json/score_json renderers (a TypedDict is not assignable to dict[str, JsonValue]). 7 tests pass, including both patch tests.
DONE remaining mypy debt: summarize_catan_board_bench_runs.py, summarize_catan_unified_benchmark.py, rescore_piece_semantic_aliases.py, render_catan_board_bench_variant.py, time_live_game.py (cast to cle.replay.contracts.GameEngineHolder, the repo's own contract for reading the engine off the viewer's `object`-typed sandbox). Deduplicated exact_two_sided_binomial_p (unified now imports the runs version) to bring unified back under 300 lines.
FIXED A LATENT BREAK: scripts/board_bench/run/eval_catan_text_format_optimization.py retargeted the evaluator by assigning evaluator.DATASET_SCHEMA etc on the package. After the split that assignment would shadow the __getattr__ forward and never reach the submodule readers, silently evaluating the wrong dataset. It now uses configured_text_evaluator(), which patches dataset_config.
FINAL: scripts/ ruff 62->0, structure 17->0, mypy 237->7 (5 fastapi-not-installed in deploy_glm_api.py + 2 pre-existing Modal errors in probes/vlm_benchmark_modal.py, both reproduce on HEAD).
DONE scripts/README.md: refreshed the direct-file counts, dropped the now-false "package initializers do not import their scripts", and documented the three packages whose structure is load-bearing (full_graph_formats dataset_config forwarding, tile_prompt_ablation scorer-in-__init__, httpx re-export for patch targets).
VERIFY FINAL: ruff 0; structure 0; mypy 7 (all documented: 5 fastapi-not-installed, 2 pre-existing Modal). pytest over tests/quality tests/evals tests/audits + every scripts-referencing test: 596 passed, 35 skipped, 7 failed, 11 errors. All 7 failures are tests/sft/** (tracebacks in sft/launchers/qwen_series/_vision_support.py; no scripts/ import) and all 11 errors are tests/data_pipeline/.../test_reweight_node_edge.py (rows_for_state() unexpected kwarg 'full_coverage' inside data_pipeline) - both match the recorded pre-existing not-mine set. tests/quality + tests/evals alone: 132 passed, 1 skipped, 0 failed. All 16 split CLIs answer --help. git diff --check clean.
